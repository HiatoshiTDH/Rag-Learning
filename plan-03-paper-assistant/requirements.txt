anthropic
voyageai
qdrant-client
requests          # calls GROBID + the arXiv API
lxml              # parses TEI XML from GROBID
rank-bm25         # BM25 for hybrid search
python-dotenv     # reads .env
pytest

# Local-free option (replaces Voyage) — uncomment to use:
# sentence-transformers   # bge-m3 (embedding) + bge-reranker-v2-m3 (re-rank)
